apiVersion: v1
kind: Service
metadata:
  name: {{ include "newsies-api.fullname" . }}
  labels:
    {{- include "newsies-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "newsies-api.selectorLabels" . | nindent 4 }}
